#!/bin/sh
set -eu

case ${1-} in
  popup|display-popup)
    command_name=$1
    shift
    exec "$WORKBENCH_REAL_TMUX" "$command_name" \
      -c "$WORKBENCH_TARGET_CLIENT" "$@"
    ;;
  *)
    exec "$WORKBENCH_REAL_TMUX" "$@"
    ;;
esac
